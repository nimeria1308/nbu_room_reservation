<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta http-equiv="x-ua-compatible" content="IE=edge" />
    <title><?= $this->title ?></title>
    <link rel="stylesheet" type="text/css" href="/resources/stylesheets/common.css" />
    <?php foreach (($this->head_elements ? $this->head_elements : []) as $e) {
        echo $e;
    }
    ?>

    <?php foreach (($this->stylesheets ? $this->stylesheets : []) as $e) { ?>
        <link rel="stylesheet" type="text/css" href="/resources/stylesheets/<?= $e ?>" />
    <?php } ?>

    <?php foreach (($this->scripts ? $this->scripts : []) as $e) { ?>
        <script src="/resources/scripts/<?= $e ?>"></script>
    <?php } ?>
</head>

<body>
    <header>
        <div>
            <h1><?= $this->title ?></h1>
            <h4><a href="">Списък на залите</a> | <a href="">Семинарна зала</a> | <a href="">Заявки</a></h4>
        </div>
    </header>
    <div id="content">
