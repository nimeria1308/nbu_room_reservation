<form method="get">
    <label for="search">Търсене на събития</label>
    <input type="text" name="search" size="40" />
    <input type="submit" value="търси" />
    <?php if ($this->search) {?>
        <input type="button" value="покажи всички" onclick="location.replace('/rooms/<?= $this->room_id ?>/events');" />
    <?php } ?>
</form>

<form method="post" action="/delete_events" id="events_form">
    <table>
        <tr>
            <th>
                <!-- selector -->
            </th>
            <th>
                Номер
            </th>
            <th>
                Клеймо
            </th>
            <th>
                Дата
            </th>
            <th>
                Начален час
            </th>
            <th>
                Краен час
            </th>
            <th>
                Организатор
            </th>
            <th>
                Наименование
            </th>
            <th>
                Мултимедия
            </th>
            <th>
                Лице за контакт
            </th>
            <th>
                Телефон
            </th>
            <th>
                Имейл
            </th>
            <th>
                Допълнителна информация
            </th>
            <th>
                Повтаряемост
            </th>
            <th>
                Действия
            </th>
        </tr>
        <?php foreach ($this->events as $e) { ?>
            <tr>
                <td>
                    <input type="checkbox" name="event[]" value="<?= $e['id'] ?>" onchange="select_event(this)"></checkbox>
                </td>
                <td>
                    <?= $e['id'] ?>
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <?= $e['start']->format("d/m/Y") ?>
                </td>
                <td>
                    <?= $e['start']->format("H:s") ?>
                </td>
                <td>
                    <?= $e['end']->format("H:s") ?>
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <?= $e['title'] ?>
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <!-- TODO: needed from backend -->
                </td>
                <td>
                    <a href="" onclick="open_show_event('<?= $e['id'] ?>'); return false;">Разгледай</a>
                    <a href="" onclick="open_edit_event('<?= $e['id'] ?>'); return false;">Редактирай</a>
                    <a href="/delete_events?room_id=<?= $this->room_id ?>&event=<?= $e['id'] ?>" onclick="return confirm_delete_event();">Изтрии</a>
                </td>
            </tr>
        <?php } ?>
    </table>
    <input type="hidden" value="<?= $this->room_id ?>" name="room_id" />
    <input type="button" value="Принтирай" onclick="print();" />
    <input type="submit" value="Изтрий избраните" onclick="return confirm_delete_events();" />
</form>